#include <bits/stdc++.h>
using namespace std;
int main()
{   std::ios::sync_with_stdio(false);
    int N,flag=1;
    while(flag)
    {
        cin>>N;
        if(N==42)
        {flag=0;break;}
        cout<<N<<endl;
    }
    return 0;
}
